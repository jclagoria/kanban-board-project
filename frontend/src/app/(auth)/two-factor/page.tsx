'use client'

import LoginPage from '@/features/auth/components/wireframes/LoginPage'

export default function TwoFactor() {
  return (
    <div>
      <div style={{ padding: 8, background: '#fff3cd', color: '#856404', fontSize: 13, textAlign: 'center', borderBottom: '1px solid #ffc107' }}>
        2FA wireframe pending — showing login as placeholder
      </div>
      <LoginPage showStructure={false} />
    </div>
  )
}
